# Outline

Planning docs for new setting content before drafting: what a new nation,
faction, hero, or history entry needs to cover, how it should sit inside the
existing `core/setting/` chapter shape (Overview/Key Details, etc. - see
`kb/styles/voice.md`), and which `kb/` entries it will touch on landing.
