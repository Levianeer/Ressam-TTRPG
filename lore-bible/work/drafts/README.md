# Drafts

Staging area for new setting content on its way into `core/setting/` (or, if
back in scope later, `core/character/races/`). Not a permanent home - once
approved, the content moves into canon and this draft is either deleted or
kept only as a dated record if the author wants a paper trail.
