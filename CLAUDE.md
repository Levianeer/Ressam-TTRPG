# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repository is

This is **Ressam**, a tabletop RPG (TTRPG) rulebook written in Markdown. The repository is content/rules-authoring - edits to game design text, tables, and formulas, not software. Every file under `core/` is plain hand-authored Markdown, edited directly - there is no build step or generated content anymore. (A prior version of this repo rendered the weapons/armor tables, spell lists, feat lists, and progression table from structured YAML via a Python + Jinja2 pipeline; that pipeline was removed once prototyping wrapped up, and its output was kept in place as ordinary Markdown.)

## Repository structure

- `core/core_rules.md` - foundational resolution mechanics: dice, attributes, skills, checks vs. Ward. **2026-08-23: the DC scale was recut from nine tiers to seven** on even 2-point steps (`4 / 6 / 8 / 10 / 12 / 14 / 16`, Very Easy through Legendary) - every roll is `1d12` plus one 0-5 number, so the ceiling is 17, **16 is the hardest DC in the game**, and 2 points of DC is exactly one step of one in six. *Tricky* and *Incredibly Hard* were cut as redundant and *Impossible* (DC 20, unreachable by anything) became a rule instead of a number: a DC more than 12 over the roller's modifier is a ruling, not a roll. The same section adds that **a Ward takes a lower DC than a Skill Check** for the same fiction - a Check is opted into with training you bought, a Ward lands on whichever Attribute the danger names, and the array leaves most characters with two 0s - so set Wards one tier lower and keep unannounced hazards at Very Hard (12) or below. Read this first; nearly every other chapter assumes these definitions. Two sibling chapters below split off what used to live here once the single file grew too dense: `rest_and_survival.md` and `exploration/stealth_and_light.md`. Basic Moves moved out to `combat/` in a 2026-08-10 reorg and were **deleted outright** by the 2026-08-23 Exchange merge (see the `core/combat/` bullet) - Dashing and Disengage survive in `positioning.md`, Grappling/Shoving/Disarming/Feint became Openings or attack types in `exchange.md`, and Shift Measure, Blind and Taunting are gone. **The `Contested Check` branch of Checks vs. Ward is now unused** - all four of its examples were combat maneuvers this rework deleted - and is kept explicitly labelled as an adjudication shape nothing calls.
- `core/rest_and_survival.md` - **2026-09-02: merged from two files, `wounds_and_survival.md` and `carrying_and_resting.md`**, into one "logistics and consequences" chapter, split out of `core_rules.md`. Slots (carrying capacity, Size modifiers, Slot Costs) and Resting (Short/Field/Long Rest ladder) open the file, followed by Damage Types (Physical/Elemental/Occult, Resistance/Vulnerability/Immunity), Wounds and Survival (Wound Penalty, Dying, Coup de Grace, Stabilization, Scars, Falling, Food and Water, Suffocation, Patched Wounds, Wound Recovery), Trauma, and Conditions (the full status-effect table - Bleeding, Blinded, Paralyzed, and so on; moved in from `combat.md` in the 2026-08-10 reorg since Conditions are referenced repo-wide by Feats, spells, and the bestiary, not just in fights). **2026-08-23 (Exchange merge):** the STR-keyed Wound Threshold table collapsed to one flat table for every creature (1-9 / 10-18 / 19+), **Off-Balance** was added to Conditions, and seven Conditions were rewritten - Blinded now reaches Parries and lost its attacks-against-have-Advantage half, Prone costs a whole Move to stand and Parries at no penalty, Stunned halves your Tempo Pool refill, Grappled ends on Prone, Incapacitated has no pool, and the Paralyzed/Unconscious auto-crit clauses were cut. **2026-08-30 (Dent/Rend Line rework):** the flat Wound Threshold table (1-9 / 10-18 / 19+) is deleted outright - a landing hit now reads straight off the target's armor (`armor.md`'s Dent Line / Rend Line) instead of a damage-after-AR lookup, and an unarmored target simply takes a flat 2 Wounds on every landing hit, no roll. Per-hit Wound severity is capped at 2, not 3.
- `core/character/` - building and advancing a character: `character_creation.md` (step-by-step guide), `attributes_and_skills.md` (reference glossary: what each of the 6 Attributes represents and what each Skill covers, grouped by category - the mechanics that use these numbers, like the Skill Check formula and DC tiers, stay in `core_rules.md` and are linked from here rather than duplicated), `knowledge.md` (what your character *knows*, as distinct from what they can *do* - a separate 24-subject, 3-tier None/Familiarity/Expertise system with no roll ever, chosen once at chargen and funded by a flat Freebie Pool plus curated Race/Career grants), `races/` (one hand-authored file per race, indexed by `races_overview.md` - see below), `careers.md`, `languages.md`, `progression_&_rewards.md` (the per-level advancement table, plus static prose covering Rewards & Treasure and Itemization). **2026-08-11/12: 8 Attributes became 6** - END merged into STR and PRE into DEX, leaving STR / DEX / MIND / ARC / FAI / CHA. The Skill list went the other way and split *further* rather than consolidating: per-weapon STR melee Skills (Two-Handed Blades, Cleaving Blades, Hafted & Polearms, Daggers & Wrestling) with **Fencing Blades** under DEX, **Ranged** and **Thrown** split apart, one Skill per Arcane and Divine school rather than a merged Arcanism/Devotion, **Thaumaturgy** absorbing Alchemy + Enchanting + Spell Crafting (Spell Crafting itself was deleted outright 2026-08-29 - see the `core/magic/` bullet below), and a new **Wayfaring**. **2026-08-23 (Exchange merge)** repointed the two combat Attributes at the Tempo Pool - **STR sizes your Tempo Die and is the whole of melee damage** (finesse blades included; only missiles take DEX), **DEX counts your dice** (`DEX \+ 1`) and buys nothing else, and **Initiative moved to MIND** (`5 \+ MIND`, static, never rolled). Wound Thresholds came off STR entirely. Athletics kept only the grapple-escape roll, Acrobatics lost its Shove/Grapple defense role, and Manipulate/Insight lost their Feint role (a Feint is now an attack type). **2026-08-23 (progression rework):** the Attributes rung of the priority ladder stopped being a point pool and became a **standard array** per letter (A `3, 2, 2, 1, 1, 0` down to E `1, 1, 1, 1, 0, 0`), assigned one number per Attribute with nothing to buy - much smaller numbers than the old 24/21/18/15/12 pool, which could only ever spend 18 anyway against a cap of 3. Attribute growth is a flat **\+1 to one Attribute at levels 4, 8 and 12**, hard cap 5, and the staged Attribute Maximum table is gone. **Attributes may now sit below 0** (the "below 0 and the character dies" rule is deleted; nothing damages an Attribute, so the only source is a racial \-1 on an array 0), with **Tempo Pool floored at 1 die and Mana/Slots at 0**.
  - `core/character/races/` - `races_overview.md` (hand-authored: General Rules Notes, Race Tiers A-E, and an index of races grouped by Artefolk/Cursed/Mystik/Night-Terror) plus one hand-authored file per race (`aelves.md`, `dwergaz.md`, `humans.md`, `orkhs.md`, `alsahli.md`, `feliids.md`, `skeggs.md`, `tembels.md`, `khoridae.md`, `dzinari.md`, `golems.md`, `tapio.md`, `daemons.md`, `varulf.md`, `strygs.md`). This split (one page per race, mirroring `core/magic/arcane/` and `core/magic/divine/`) exists to make individual races easier to manage and to leave room for per-race lore.
- `core/combat/` - three files as of the **2026-08-23 Exchange merge**, which replaced the Oppose/Reaction system wholesale. The design log for the whole rework is `exchange_log.md` at the repo root (61 numbered items; item 62 records the merge). Read `exchange.md` first - the other two assume it.
  - `exchange.md` - **melee combat entire, and the file that matters.** One resource, the **Tempo Pool** (`DEX \+ 1` dice, each sized by STR, refilling at the start of your turn), pays for every attack, every defense, every Opportunity Attack and every off-turn Feat; **there are no Reactions and no free defense of any kind**. An Exchange is one attack and its answer: attacker rolls `1d(Tempo Die) \+ Weapon Skill \+ Distance` under one of three types (Feint / Press / Measured, declared and paid for the whole sequence up front), defender either spends a die on a **Parry** or the blow simply lands; higher total wins, ties to the defender, **margin 5+ takes an Opening** (Riposte, Called Shot, Disarm, Shove, Grapple, Break Away, or a Feat) - **4 or 3 with a sharp weapon**, per `weapons.md`'s Opening column. **There are no critical hits anywhere in Ressam**: the margin-10 crit was deleted at the merge (it measured 0.00% of landed blows below STR 5, since a margin that large is unreachable between equals and a roll big enough to earn it is one nobody answers, which fixes it at 5), and `exchange.md` keeps a section arguing the deletion because players will look for the rule. Every melee hit that lands costs the target a Tempo Die (**Shock**), which is what makes fights tip. Also owns: the four **Measure Bands** (Close/Middle at Adjacent, Long/Far at Reach, `\-2` one Band off, no roll two Bands off) and the Far Band's strike-past-an-intervening-creature clause; **Initiative \= 5 \+ MIND**, static and never rolled; Mythic Initiative's core rule; Surprise's two rungs (Caught Out / Ambushed); ranged combat, which is **not** an Exchange at all (`1d(Tempo Die) \+ Ranged or Thrown` vs. a situational **Shot DC**, unanswerable, no Opening, no Shock) and its **Loading** ladder, which is the only thing capping rate of fire.
  - `combat.md` - what's left of the old chapter: action economy (no Reaction row), the damage roll (**melee damage is STR for every weapon and Skill without exception; missiles take DEX**), a *There Are No Critical Hits* section that points at `exchange.md`, and armor's in-fight behavior.
  - `positioning.md` - the grid, movement, and Cover. Cover stays **binary** and untouched - that was ruled deliberately in log item 58, and the Far Band was repaid out of the intervening square instead of out of graduated cover.
  - **Deleted by the merge:** `maneuvers.md` (Oppose, Stance, Effects, the Press, Reactive Casting) and `basic_moves.md`.

- `core/magic/` - spellcasting rules split into `laws_of_magic.md` (the in-fiction physics every spell, ritual, and minor working answers to - Five Laws that bound what magic can ever do (no true creation from nothing, no touching past/future, no making/unmaking/growing a soul, no rewriting essential nature by mortal craft, no effect without a thread of sight/touch/sympathy), Three Costs that price what's lawful but costly (Conservation, Signature, Reciprocity), and an Adjudication section for when the spell lists are silent on a player's novel idea - read this before ruling on anything not covered by an existing spell), `magic_overview.md` (universal rules shared by both paths), and `minor_magic.md` (small, instinctive Mana effects - minimum 1 Mana, scaling in scale/duration from there - that fall outside a normal spell), with two parallel school trees. **`spell_crafting.md` was deleted 2026-08-29** - the VERB+NOUN+MODIFIERS point-budget system for building custom spells was judged too much arithmetic load for what it bought (see `DESIGN_GUIDE.md`'s Elegance pillar) and cut outright rather than reworked; `TODO.md` folds what it was covering for (thin per-school spell counts) into the spell-list rebalance pass instead:
  - `magic/arcane/` - arcane schools (aeromancy, geomancy, hydromancy, pyromancy, shadowmancy) plus `arcane_overview.md`.
  - `magic/divine/` - divine schools (benediction, cultivation, invocation, necration, subjugation) plus `divine_overview.md`.
- `core/feats/` - feat categories (general, martial, magic, prestige, skill, racial), indexed by `feats_overview.md`. **2026-09-14: Arcane Feats and Divine Feats merged into `magic_feats.md`** as part of the magic rework (see the `core/magic/` bullet below) - the category list above reflects this; historical notes elsewhere in this bullet that still say "arcane, divine" predate the merge. The same pass **deleted Thane's Brand from `prestige_feats.md` outright** (its whole premise - trading scroll-scribing for rune-branding - depended on scribing, which no longer exists for any school): Prestige Feats are down to 7, not 8. **2026-08-23: every prerequisite was re-cut against the standard array** (26 changed), and `feats_overview.md` now states the ceiling that keeps them honest: **no ordinary Feat asks for an Attribute above 2, no Feat asks for more than `\+3` ranks in a Skill, and a Prestige Feat may ask for 3 and no more** - because a prerequisite is a level gate in disguise (outside an **A** you start at 2, reach 3 at Level 4, 4 at Level 8), and **a Skill Rank requirement is an Attribute requirement too**, since Rank <= governing Attribute. To make a Feat harder to get, add a second requirement, never a bigger number. Every `4\+` gate is gone from the book; 27 Feats are live at Level 1, 24 at Level 4, and all Prestige Feats (8 as of this rule, now 7 post-2026-09-14) at Level 6 (the first Feat grant after their Level 5 unlock). `racial_feats.md` (added 2026-08-10) holds racial traits strong enough to cost a Feat slot instead of coming free with that race - grouped by race, gated by a `Race: <Name>` prerequisite. Currently holds two demoted death-cheat-tier Features: Humans' Indomitable Spirit (Tier E, unpaired - just an outright free-tier outlier) and Feliids' Nine Lives (Tier B, dropped to Tier D once Nine Lives left - its paired drawback Reckless Instinct was removed with it rather than left stranded, and Feliids got a new small Feature, Magnetic Charm, to backfill the vacated slot).
- `core/equipment/` - `armor.md`, `weapons.md`, `supplies.md`, and `alchemy.md` (crafting oils/bombs/salves from ingredients). **2026-08-11:** Shields (the Shield Table/Descriptions/Guard rules, formerly `armor.md`'s own section) moved to `weapons.md` - a shield isn't armor, it's something you actively wield in a hand, and unlike armor it can now also be swung: a new **Shield Bash** attack (1d4 Blunt, Close Band) uses the **Daggers & Wrestling Skill**, same as an unarmed strike. Shield repair still runs through `armor.md`'s Crafting rules (shields share durability with armor, no separate track). **2026-08-23 (Exchange merge):** `weapons.md`'s Reach column became the four Measure Bands, the Critical column became a **Crit** margin threshold (`\-` = 10, or 9 / 8), a **Reload (X)** property replaced the old ad-hoc reload rules on every missile weapon, and the properties that no longer resolved were cut or converted (Lashing Reach, Trip and the Weighted Chain's grapple deleted; Chain Flail's "Bypasses STR Ward" became **Ignores Guard**; the Brawling table lost its Grapple row and Headbutt's Stun). **Guard now adds to every Parry you make** while a shield is equipped and **only the highest counts** - which replaced the two-shield Shield Cap outright - and the Pavise lost its Guard rating in favor of being solid obstruction. `armor.md`'s Penalty column was rebanded off AR alone (1-4 / 5-7 / 8-10 = \-1 / \-2 / \-3), **nothing in a fight reads Penalty any more**, nothing reduces it (the `Broken In` Feat was deleted), and Rigid armor gained its one in-fight behavior: it loses no durability to a blow it absorbs entirely. **2026-08-30 (Dent/Rend Line rework):** the single AR number is gone. `armor.md` now prints two values per armor, **Dent Line and Rend Line** (`Wound = old AR + 1`, `Deep = Wound + 4` - Gambeson 3/7, Chain Mail 6/10, Full Plate 9/13), and a landing hit rolls weapon dice + STR/DEX and compares the total against them instead of subtracting AR: below Wound is **Turned Aside** (armor chips 1 durability, 0 for Rigid), at/above Wound is 1 Wound, at/above Deep is 2. **Every die at its maximum face finds a gap** - a fixed die-face exception to "no critical hits," not a margin bonus - upgrading a Turned Aside result to 1 Wound, or adding Bleeding on top of a hit that already landed. Durability starts equal to Dent Line and both lines fall together as it drops; at 0 durability the wearer resolves as **Unarmored**, which is now also the rule for anyone with no armor at all: a flat 2 Wounds on every landing hit, no roll. Penalty's band shifted with it (3-5 / 6-8 / 9-11), but its values and Rigid/Flexible's rules are otherwise unchanged. Every other file granting a flat `+X AR` bonus or an ignores/bypasses-AR effect - Feats, spells, races, alchemy, NPC/mount/follower stat blocks, and the character sheet (`character_sheet.html`/`sheet_test.js`) - was converted in the same pass (`+X AR` becomes `+X` to both lines together; "ignores/bypasses N AR" becomes lowering both lines by N for that attack; "degrades armor by N AR" becomes costing N durability). `core/bestiary/` was not touched, same as every prior combat rework - see `exchange_log.md` item 66.
- `core/exploration/` - travel and downtime layer: `traveling.md` (hex travel, mounts), `downtime.md` (revelry/celebrations), `leadership.md` (followers and cohorts), `dungeon_turns.md` (the 10-minute Turn as the clock for dungeon/site exploration - light tracking and wandering encounters share one party-wide tally), and `stealth_and_light.md` (Stealth and Hiding, Light and Vision - split out of `core_rules.md` and placed here since Light and Vision's burn-duration table already lived in lockstep with `dungeon_turns.md`'s Light Tracker). These feed the core loop directly, not optional flavor: Traveling is how the party reaches a dungeon and burns rations, Leadership is how a growing operation delves deeper without resurfacing, and Downtime is where rewards from delving cycle back into progression.
- `core/setting/` - the world of Ressam itself: `setting_overview.md` (welcome blurb, currency, language, and an index of this chapter - the `races_overview.md`/`feats_overview.md` pattern), `calendar.md` (the seasonal year and its festivals), `history.md` (Age of Mythos, the Sundering, Age of Revival, and the Third Age's wars up to 402 RC), `geography.md` (the Mourning Star and twin moons, the continents, and the Mortal Plane/Fields of Nocturne), `nations.md` (the great powers, the nation relations table, and global/local factions), `religions.md` (the faiths practiced across Ressam), and `heroes.md` (legendary figures). Cross-references a lot with `core/character/races/` - each race's own page covers that race's culture and mythology in more depth than fits here.
- `DESIGN_GUIDE.md` - the yardstick for evaluating any mechanic, item, spell, or rule change. **Read the three subsections under Scarcity before writing any number a player rolls against or has to reach**: they cover why a Feat slot is the cost and the prerequisite is only flavor, why the `1d12 \+ (0-5)` ceiling of 17 has to be checked before setting a DC or a threshold, and why a Ward must not be priced like a Skill Check. They also record the **5th Edition mirror** - Ressam's Attributes now map onto 5e's ability *modifiers* almost exactly (2 = a 14-15, 5 = a 20), and 5e's array in modifier terms is nearly our **C**'s, which makes 5e a usable sanity check for numbers of this shape: notably, every ability-score prerequisite in 5e is exactly 13, a \+1, and never higher. Five Design Pillars (Realism as "feel, don't simulate," Elegance, Scarcity, Niches, and Distinction between Martials and Casters), followed by Spell Design Guidelines covering damage caps (`Xd8 x Mana`, Pyromancy alone at `Xd10 x Mana`), the no-save-or-suck rule for Overcome spells, summoning being near-exclusive to Necromancy, per-school distinctiveness, Faith always being required for divine magic, healing defaulting to Patched Wounds, spells not bypassing mechanics for free, and Gish/spellblade playstyles being a Feat-driven compromise rather than something magic grants directly. Read this before proposing or reviewing changes to `core/magic/`, `core/feats/`, or `core/equipment/` - it's the reasoning behind *why* those files are shaped the way they are, not just what's in them. Several of its Spell Design Guidelines (Creation, Healing, Summoning, Faith, Bypass) are the mechanical/production-side expression of Laws `laws_of_magic.md` states in-fiction (Borrowed Substance, Conservation, the Soul, Reciprocity) - the two files should stay consistent with each other when either changes. **2026-09-14: also gained a Prose Style section** - unlike the rest of the file, this one applies to every chapter under `core/`, not just magic/feats/equipment; see the Content conventions section below and `TODO.md` for rollout status.
- `tools/` - Python, standalone (stdlib only, no install step). **STALE AS OF THE 2026-08-23 EXCHANGE MERGE, and deliberately left that way:** `combat_engine.py`, `creature_rating.py` and `encounter_rating.py` all model the deleted Oppose/Reaction system and their numbers mean nothing about the current rules. `tools/tempo_sim.py` is what actually measured the Exchange (see `exchange_log.md` items 33-61) and was, at the 2026-08-23 merge, the only file here whose model matched the merged rules - **it is now ALSO stale as of the 2026-08-30 single-attack ruling** (`core/combat/exchange.md`'s "Attacking" section): it models multiple declared attacks per turn (`seq_cost`, `max_attacks`, "attacks declared per turn", Riposte cancelling a queued attack), a mechanic that ruling deleted in favor of one attack per Major Action with any number of Tempo Dice invested and summed into it. Its shot/reload modeling is unaffected (ranged didn't change). Rebuilding this directory against the current rules is open work; until it is done, do not quote a number from any of these tools. The description below is of what they *were*. - the computational counterpart to `DESIGN_GUIDE.md`'s qualitative pillars: run these to check whether a mechanical change actually lands the way it reads on paper, rather than eyeballing it. Two independent pieces:
  - `power_score.py` - prices a creature backward through `progression_&_rewards.md`'s own Attribute/Skill/Feat advancement table to find the lowest PC Level that could legally afford it (its "Effective Level"), and reads an XP reward straight off that table - no borrowed CR-style curve, no simulation, just point-budget accounting. Cheap enough to run on every bestiary entry.
  - `combat_engine.py` (a standalone Oppose/Effect/Press resolution engine - dice, Attack Roll vs. Evasion, Margin table, Shield Guard, the Strike/Shift/Control/Recover Effect menu, Press chains, Mythic Initiative - matching `core/combat/maneuvers.md` and `combat.md` as of the 2026-08-09 rework) backs two Monte Carlo rating tools, `creature_rating.py` (1-on-1) and `encounter_rating.py` (N monsters vs. a 4-PC party). **Why this shape, not a raw number or a D\&D-style CR curve:** Ressam PCs don't scale much across Level 1-12 (progression_&_rewards.md's Attribute-point budget only moves from 18 to 21 over the whole ladder), so both tools instead simulate many fights against one fixed reference build, `combat_engine.BASELINE_PC`, and bucket the win rate into a 5-tier rating - **Much Worse / Worse / Equal / Better / Much Better** - that stays meaningful at any Level rather than needing a level-scaled baseline. Run them (`python3 tools/creature_rating.py`, `python3 tools/encounter_rating.py`) after any change to `core/combat/`, `core/equipment/weapons.md` or `armor.md`, or a creature's Attributes/Skills/gear, to see whether the change actually shifted the tier you meant it to. The `bestiary-npc` skill (`.claude/skills/`) already wires both into its Validate stage when generating `core/bestiary/*.md` entries.
- `CREDITS.md` - attribution and licensing note.
- `TODO.md` - planning doc for in-progress, cross-session rework items, when one exists. Only exists while something is actively open; gets deleted once that item is fully resolved and merged, so its absence means "nothing outstanding" rather than an oversight. Check it (and recent git log/diff) before touching feats, combat, or equipment mechanics - a change already in flight may be scoped and reasoned here.
- `assets/` - currently empty (reserved for future images/handouts).
- `templates/character/character_sheet.html` - a hand-authored, self-contained interactive player character sheet (plain HTML/CSS/JS, no build step, no dependency on anything else in the repo) - a player fills in race/career/feats/spells/equipment by hand. Open it directly in a browser to use it.
- `templates/character/sheet_test.js` - **the only automated test in the repo**, and the only JavaScript. It loads the sheet into a real DOM (jsdom), drives it with events, and asserts every derived stat against the rules - the Tempo Pool ladder, Initiative, Armor Penalty banding, Shield-Guard-takes-the-highest, per-weapon Parry, the Opening threshold column - plus that a character saved before the 2026-08-23 Exchange rework still loads with its dropped fields ignored and its derived stats recomputed. **jsdom is deliberately not vendored**: there is no `package.json` and no `node_modules` here (both are gitignored), so install it outside the repo and pass `NODE_PATH` - the file's header has the four commands, and running it without jsdom prints them and exits 2. Run it after any edit to the sheet.
- `templates/Home.md` and `templates/_Sidebar.md` - local reference copies of the two GitHub Wiki pages that are hand-edited directly in the wiki UI (see "Publishing: GitHub Wiki" below); kept here for convenience when drafting changes before pasting them into the wiki.
- `.github/workflows/sync-wiki.yml` - CI that publishes `core/` to the GitHub Wiki; see "Publishing: GitHub Wiki" below.
- `README.md` - short public-facing entry point (project blurb + link to the GitHub Wiki).

## Publishing: GitHub Wiki

The rulebook is published as a GitHub Wiki, auto-synced from `core/` on every push to `main` via `.github/workflows/sync-wiki.yml`: the workflow clones the wiki's own git repo (`<repo>.wiki.git`), mirrors `core/`'s contents into it 1:1 (folder structure preserved, `core/` prefix stripped), and pushes only if something changed. **Never hand-edit wiki pages that correspond to a `core/**/*.md` file** - edits made directly in the wiki UI are silently overwritten on the next sync. Edit `core/` and let CI publish it.

Two pages exist **only** in the wiki repo, not in this one, and *are* meant to be hand-edited directly in the wiki UI: `Home.md` (landing page) and `_Sidebar.md` (persistent nav, shown on every page). The sync workflow explicitly excludes both from its mirror/delete step so it never overwrites them. Both list every chapter grouped by section, in the same order as "Repository structure" above. Local reference copies live at `templates/Home.md` and `templates/_Sidebar.md` for convenience - edit those first, then paste the result into the wiki UI. When a chapter is added, renamed, or removed under `core/`, update both pages (and their local copies) to match.

**GitHub Wiki quirk to know before editing either page:** wiki page URLs/links are flat and basename-only, regardless of which subfolder a file is mirrored into - e.g. `core/magic/arcane/pyromancy.md` is mirrored to `magic/arcane/pyromancy.md` in the wiki repo but renders at `/wiki/pyromancy`, not `/wiki/magic/arcane/pyromancy`. Multi-level nested paths in Markdown links (e.g. `[Pyromancy](magic/arcane/pyromancy)`) 404. Always link to wiki pages by bare filename with no folder prefix and no `.md` extension, e.g. `[Pyromancy](pyromancy)`. This only works because every `core/**/*.md` basename is unique repo-wide - if that ever stops being true, this linking scheme needs to be revisited.

## Content conventions

- Em Dashes (—) and En Dashes (–) are not used, instead use only Hyphens (-).
- Files are exported from Google Docs, so literal Markdown-special characters are backslash-escaped in the source (e.g. `1d12 \+ Skill Ranks \+ Attribute`, `Ressam is not Dungeons \& Dragons`, `Slots \= STR \+ END`). Preserve this escaping style when editing existing text so re-exports/diffs stay clean.
- Formulas and derived stats are given as bolded inline math (e.g. `**Maximum Wounds = Size baseline + Feats**`) rather than code blocks. Follow this pattern for new formulas.
- Section breaks use a horizontal rule (`---`) between major topics within a chapter.
- Tables are used heavily for skill lists, DC tiers, and comparative mechanics (e.g. Arcane vs. Divine casting) - match existing table shapes when adding related content instead of introducing prose where a table already exists for that topic.
- **A `[[Display Text|PageName]]` wiki link inside a table cell breaks the table** - the `|` in the link syntax reads as a column separator to the table parser. Inside a table cell, link with the pipe-free form `[[PageName]]` instead (it displays the bare page name, e.g. `[[exchange]]`) and put whatever display text is needed in the surrounding cell prose - e.g. `see Grappling in [[exchange]]`, not `see [[Grappling|exchange]]`. Outside a table this isn't an issue; `[[Display Text|PageName]]` is fine in ordinary prose.
- Core terminology is reused verbatim across chapters (e.g. `Major Action`, `Ward`, `Evasion`, `AR`, `Trauma`, `Mana Cost`) - when editing one chapter, check whether the same term/mechanic is defined or referenced elsewhere (`core_rules.md`, `rest_and_survival.md`, `combat.md`, `magic_overview.md`) so definitions stay consistent.
- The game's tone/scope per `core_rules.md`'s foreword: Ressam is a crunchy, lethal alternative to other TTRPGs, set in a world balanced around an early-1500s baseline - matchlock firearms sit alongside plate armor, magic is feared and regulated. Combat is the heart of the game; everything outside of it is meant to be played loosely, "rulings not rules."
- **2026-09-14, prose style:** `core/` text should read as a table reference, not a teaching text - state each rule once, in the fewest words that remove ambiguity. Cut sentences that explain *why* a rule is shaped the way it is (that belongs in `DESIGN_GUIDE.md`/`exchange_log.md`, not `core/`) unless removing the sentence would let a reader reasonably misapply the rule - see `DESIGN_GUIDE.md`'s Prose Style section for the full litmus test and worked examples. Default edge cases to a GM ruling instead of an enumerated sub-case, and prefer tables/bullets over prose past two comparable items. This targets prose density only, not mechanical scope - Ressam's crunch stays; the words around it don't. Piloted against `exchange.md` as a sample; not yet applied repo-wide (see `TODO.md`).